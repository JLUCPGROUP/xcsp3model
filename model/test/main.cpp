#pragma once
#include <sstream>
#include <string>

#include "XCSP3PrintCallbacks.h"
#include "BMFileParser.h"
#include "Timer.h"

using namespace cudacp;
using namespace std;

//#define LOGFILE
const int64_t  time_limit = 600000;
const string XPath = "BMPath.xml";

int main() {
	/*string bm_path;
	if (FindBMPath(XPath))
		bm_path = _bm_path;
	cout << bm_path << endl;
	HModel *hm = new HModel();
	GetHModel(bm_path, hm);
	hm->Show();
	delete hm;
	cout << "--------------------------end--------------------------" << endl;*/
	return 0;
};